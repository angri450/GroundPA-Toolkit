# GroundPA Toolkit 中文总览

GroundPA Toolkit 是一个 Claude Code 多 skill 插件。目标是让 Claude Code 通过一次 plugin 安装，获得农学生论文、文档处理和文献检索工作流所需的核心能力。

项目主线是纯 .NET / Nong CLI，不引入 Python 包。

## 主线

主线能力只保留和 GroundPA 直接相关的 skills：

| Skill | 用途 |
|-------|------|
| `word` | Word 检查、转换交接、切片、版式证据、修复和编辑 |
| `pdf` | PDF 检查、切片、`content.nongmark`、页面渲染和图片提取 |
| `literature` | 类 CNKI 检索 DSL、OpenAlex/Crossref/Unpaywall 元数据和开放获取查询、参考文献导出 |
| `inspect` | 论文诊断、结构、证据、数据需求和写作支持 |
| `excel` | Excel 读取、分组数据提取和工作簿创建 |
| `chart` | 统计分析和常见图表 |
| `diagram` | 流程图、网络图和树图 |
| `pptx` | PPTX 读取和幻灯片清单 |
| `multimodal` | OCR、图像结构检查、云端 OCR 和图片/PDF 转 Word |
| `genre` | 论文体裁和写作指导 |
| `icons` | Bioicons 查询 |

GroundPA Toolkit 2.3.1 面向 Nong 3.2.5+。`literature` 使用 3.2.5+ 已正式包含的 `nong lit` 命令。使用前通过 `nong commands --json` 确认存在 `lit parse`、`lit validate`、`lit plan`、`lit search` 和 `lit export`。

Stage19 文献提供方只包括 OpenAlex、Crossref 和 Unpaywall。全文检索、爬虫、绕过付费墙、Semantic Scholar、PubMed、PMC、arXiv、万方和自动中英同义词扩展都未实现。

## 安装

```bash
claude plugin marketplace add https://gitcode.com/angri450/GroundPA-Toolkit.git
claude plugin install groundpa-toolkit@angri450
```

安装后重启 Claude Code，或执行 `/reload-plugins`。

插件不安装 Nong CLI。首次使用 Nong-facing skills 前安装或更新：

```powershell
dotnet tool install --global Angri450.Nong.Cli --add-source https://mirrors.huaweicloud.com/repository/nuget/v3/index.json
```

## 仓库边界

可安装插件面只保留 `.claude-plugin/`、主线 skill 目录、README、manifest 和 license。Git 提交面额外保留 `log/`，用于展示开发过程。

本地开发规则、旧脚本、通用开发 skills、上游完整工程、构建输出和临时包都放到仓库外的 `../GroundPA-Toolkit_archive/`。不要把仓库内 `_archive/` 留给 `nong skill scan` 扫描；`.gitignore` 只负责防止误拷回仓库后被提交。

`log/` 要提交。它用于展示开发过程。
